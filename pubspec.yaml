name: markerplace_sales_monitor
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  animated_toggle_switch: ^0.8.3
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6  
  flutter_dotenv: ^5.2.1
  http: ^1.2.2
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.12
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.8.0
  lints: ^5.1.0
  test: ^1.25.7

flutter:
  uses-material-design: true
  assets:
    - .env